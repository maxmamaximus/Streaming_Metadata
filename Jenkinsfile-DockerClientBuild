pipeline {
    agent any
    stages {
        stage('Docker Build') {
            steps {
                script {
                    docker.build('doc_client_test')
                }

            }
        }
        stage('Run Docker Container') {
            steps {
                script {
                    docker.image("doc_client_test").run()
                }
            }
        }
    }
}