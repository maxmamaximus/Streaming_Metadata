pipeline {
    agent any
    stages {
        stage('Docker Build') {
            steps {
                script {
                    docker.build('doc_test_build1')
                }

            }
        }
        stage('Run Docker Container') {
            steps {
                script {
                    docker.image("doc_test_build1").run()
                }
            }
        }
    }
}